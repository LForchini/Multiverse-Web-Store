name: Test

on:
  pull_request

jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - name: Test server
        run: cd server && npm test
      
      -name: Test client
        run: cd client && npm test